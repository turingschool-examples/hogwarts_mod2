<h2>Students of <%= "#{@professor.name}" %></h2>

<ul>
  <% @professor.students.each do |student| %>
    <li><%= student.name %></li>
  <% end %>
</ul>

<h4>Students' Average Age: <%= @professor.students_avg_age %></h4>