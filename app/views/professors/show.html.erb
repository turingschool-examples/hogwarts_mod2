<section class="students">
  <h3>Students</h3>
  <ul>
  <% @students.each do |student| %>
    <div class="student" id="student-<%= student.id %>">
      <li><%= student.name %></li>
    </div>
  <% end %>
  </ul>
</section>
