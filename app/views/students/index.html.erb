<h2>Students Index</h2>

<ul id='student-list'>
  <% @students.sort_alphabetically_by_name.each do |student| %>
    <li id="student-<%= student.id %>">
      <%= student.name %>: <%= student.professors.count %>
    </li>
  <% end %>
</ul>
