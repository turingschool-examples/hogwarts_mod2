<h1>Students</h1>

<section>
  <h3>Students and Number of Professors</h3>
  <% @students.each do |student| %>
    <section class="student">
      <p><%= student.name%>: <%= student.num_professors %></p>
    </section>
  <% end %>
</section>